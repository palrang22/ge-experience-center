# 결정이 필요한 항목 (2026-09-17)

`plan.md`를 진행하기 전에 아래 항목들을 확인해야 합니다. 각 질문 아래 답변란에 선택지를 적거나 자유롭게 써주세요.

---

## Q1. 화면 전환을 메뉴 어디에 둘까요?

40인치 세로 모니터라 위아래 공간은 넉넉하지만 가로 폭은 제한적입니다.

- **A. 상단 메뉴** — 기존 사이트와 비슷한 느낌, 가로 폭을 일부 차지
- **B. 좌측(또는 우측) 사이드 메뉴** — 세로 화면에서 항상 눈에 보이지만 본문 가로 폭이 줄어듦
- **C. 상단에 큼직한 3개 탭 버튼** (아이콘+텍스트, 터치 친화적)

**답변:** A

---

## Q2. 화면 전환 방식

- **A. URL 라우트 분리** (`/domains`, `/architecture`, `/playground`) — 새로고침해도 상태 유지, 나중에 "특정 화면으로 바로 열기" 같은 운영이 쉬움
- **B. 같은 화면 안에서 상태(state)만 바꿔서 전환** — 라우트 개념 없이 SPA 내부 탭처럼 동작. 키오스크라 URL이 노출될 일이 적어서 굳이 라우트가 필요 없을 수도 있음

**답변:** A

---

## Q3. "AI는 어떻게 판단하는가" (아키텍처 설명) 섹션, 이번에 어떻게 할까요?

기존 사이트는 5계층을 마우스로 드래그해서 회전시키는 3D 노드 다이어그램이었습니다. 복잡하고 세로 화면에는 잘 안 맞습니다.

- **A. 유지하되 단순화** — 3D 드래그 대신 위→아래로 흐르는 정적 다이어그램(데이터 → Gemini Enterprise → 결과물)
- **B. 아예 텍스트/아이콘 카드 3~4개로 대체** (다이어그램 없이)
- **C. 이번 1차 버전 스코프에서는 제외** — 도메인 선택 → 데모 체험 2개 화면만 우선 완성

**답변:** D. 이거 가장 나중에 구현하고, A / B 시안을 직접 보고 결정할게

---

## Q4. 데모 시나리오 축소 기준

- **A. 6개 도메인 모두 동일하게 4단계로 통일**
- **B. 도메인별로 가장 임팩트 있는 단계만 골라 3~4단계로 유동적으로** (도메인마다 단계 수가 다를 수 있음)

**답변:** B

---

## Q5. 이번 1차 버전에 포함할 도메인 범위

- **A. 6개 도메인 전부**
- **B. 우선 1~2개 도메인만 완성해서 검증 후 나머지 확장** (어떤 도메인부터 할지도 함께 알려주세요)
- 참고: 기존 사이트에는 6개 도메인 외에 "R&D 신소재 품질 검증 세트", "글로벌 원자재 MD 공급망 세트"도 있었는데, 이번 스코프에 포함할까요? (기본값: 제외)

**답변:** A

---

## Q6. 도메인 카드에 남길 보조 정보

규제 임계값 같은 숫자 태그는 빼기로 했는데, 그 자리에 무엇을 넣을까요?

- **A. 에이전트 이름 + 사용 예시만** (숫자/태그 없이 텍스트만)
- **B. 숫자 없는 한줄 임팩트 요약 추가** (예: "부서 간 데이터 단절로 숨어있던 리스크를 찾아냅니다")
- **C. 텍스트 최소화, 아이콘 중심**

**답변:** A, C 혼용 (아이콘+이름+간단예시)

---

## Q7. 외부 Gemini Enterprise 채팅 연결 방식

실제 에이전트와의 대화는 `vertexaisearch.cloud.google.com`의 외부 화면에서 이루어지고, iframe 임베드는 막혀 있는 것으로 확인됐습니다(팝업 차단 메시지로 확인). 세로 단일 모니터라 기존 "듀얼 스크린 모드"(보조 모니터 전제)는 그대로 쓰기 어렵습니다.

- **A. 같은 탭에서 이동** — 체험 후 뒤로가기로 복귀
- **B. 새 탭으로 열기** — 원래 체험 화면은 그대로 남아있음
- **C. 화면을 상/하로 분할** (위: 가이드/프롬프트, 아래: 채팅) — 세로 모니터 특성을 살리는 방식이지만 구현 난이도 있음

**답변:** 내생각에 C가 나을 것 같아. 누르면 하단에 채팅 페이지가 나오는걸로

---

## Q8. 무인 대기 시 자동 초기화(타임아웃)

줄 서서 체험하는 키오스크이므로, 한 사람이 오래 붙잡고 있거나 이탈 후 자리를 뜬 경우를 대비한 자동 리셋이 필요할지 궁금합니다.

- **A. 필요함** — 몇 초/분 미입력 시 처음 화면으로? (구체적인 시간 알려주세요)
- **B. 필요 없음** — 진행자가 수동으로 초기화

**답변:** A, 1분 미입력시 초기화면으로 (팝업 예아니오 떠야 함)

---

## Q9. 데모 첨부 파일(PDF/XLSX 등) 재사용 여부

- **A. 기존 파일 그대로 재사용**
- **B. 축소된 3~4단계 시나리오에 맞게 일부 새로 제작/편집**

**답변:** B

---

## Q10. 색/테마 방향

"AI 느낌 나는 그라데이션·빛나는 구체" 대신 사람이 짠 편집 디자인 느낌으로 가기로 했습니다(레퍼런스: Trading Landing Page/Roobinium, B2B SaaS Landing Page/HackerRank, Data-Driven Analytics Platform Landing Page, Web3 Crypto Payments Platform Landing Page — 공통적으로 다크 배경 + 단색 포인트 컬러 + 데이터/그리드 중심). 이걸 기준으로 아래를 정해주세요.

- **다크 테마 vs 라이트 테마** (전시 부스 조명, 40인치 모니터 가시성 고려)
- **포인트 컬러** — MegazoneSoft 또는 Google Cloud/Gemini 브랜드 컬러를 따를지, 별도로 정할지
- 참고 레퍼런스에서 특히 마음에 드는 부분이 있다면 자유롭게 적어주세요.

**답변:** 다크 먼저 제작, 라이트 후 제작하여 토글 가능 / 포인트 컬러는 #000000 #414AFF #8920FF #34AD5C 이거랑, 필요시 Google Cloud 색상 써도돼
