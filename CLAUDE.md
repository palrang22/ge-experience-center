# GE Experience Center (v2) — 프로젝트 컨텍스트

## 이게 뭔가

MegazoneSoft가 사내/고객 대상으로 **Gemini Enterprise(GE) Agent를 체험시키는 키오스크 웹앱**을 만든다.
설치 환경은 **40인치 세로(포트레이트) 모니터 1대**, 사용 시나리오는 전시장/부스에서 **줄을 서서 순서대로 체험**하는 방식이다.

기존에 선배가 만든 참고용 데모가 있다.
(https://ge-experience-center-294123481298.asia-northeast3.run.app/)

- 좋은 점: **6대 산업 도메인**으로 나눠서 에이전트를 소개하는 구조, 에이전트 이름과 실제 사용 예시(prompt)를 보여주는 방식.
- 문제점 (이번 프로젝트의 존재 이유):
  1. **한 페이지에 다 몰려있음.** 도메인 소개, "AI가 어떻게 판단하는가"(아키텍처 설명), 실제 체험(Agent Playground)이 전부 스크롤 앵커(`#domains`, `#architecture`, `#playground`, `#howto`)로만 구분된 하나의 롱스크롤 페이지다. 화면 전환이라는 느낌이 없어서 사용자가 지금 뭘 보고 있는지 직관적으로 파악하기 어렵다.
  2. **불필요하게 기술적인 태그.** 도메인 카드에 "DSR 규제 임계값 40%", "연체위험도 차단선 3%" 같은 실무 규제/수치 태그가 붙어 있는데, 체험객 입장에서 이해에 도움이 안 되고 오히려 진입 장벽이 된다.
  3. **데모 시나리오가 너무 길다.** 도메인당 데모 가이드가 **7단계**(Foundation → Catalog Discovery → CEO Overview → ... → CEO Board Report)로 구성되어 있고, 각 단계마다 파일 첨부, 프롬프트 복사, 채팅 실행, 확인 포인트 체크가 필요하다. 줄 서 있는 사람이 있는 키오스크 환경에서는 세션당 체류 시간이 길어져 이탈/대기 이슈가 생긴다.
  4. **실제 에이전트 대화는 외부 화면에서 일어난다.** "에이전트 채팅 시작" 버튼은 이 사이트가 아니라 `vertexaisearch.cloud.google.com/.../agent/.../session/-` 로 이동한다(iframe이 아니라 새 창/팝업). "듀얼 스크린 모드"는 보조 모니터가 있다고 가정하고 팝업창을 띄우는 방식인데, **단일 세로 모니터 키오스크에는 이 전제가 안 맞는다.** 이번 버전에서 이 연동 UX를 다시 설계해야 한다.

## 무엇을 만드는가 (방향)

1. **화면을 명확히 분리한다.** 상단(또는 사이드) 메뉴에서 "산업 도메인 선택 / AI는 어떻게 판단하는가 / 데모 체험"을 고르면 각각 별개의 화면(뷰)으로 전환되도록 한다. 지금처럼 한 페이지 스크롤로 다 보여주지 않는다.
2. **도메인 카드는 단순하게.** 에이전트 이름 + 사용 예시(prompt 한 줄)는 유지하되, 규제 임계값 같은 숫자/전문 태그는 제거한다.
3. **데모 시나리오는 3~4단계로 축소한다.** 기존 7단계 시나리오를 가장 임팩트 있는 3~4단계로 재구성한다.
4. **40인치 세로 모니터 · 줄서기 키오스크**라는 제약을 항상 최우선으로 고려한다. 목표는 "이탈 없이, 빠르게, 직관적으로" 체험을 끝내는 것이다. 화려함보다 완주율이 우선이다.

## 디자인 방향 — 그라데이션을 적극 수용한다 (2026-09-17 뒤집힘)

~~원래는 "AI 느낌"(보라-파랑 그라데이션 블롭, glassmorphism 등)을 피하고 절제된 단색+그리드로 가려 했음~~ → **사용자가 이 제약을 폐기하고 정반대로 확정**: 이제 그라데이션을 화려하게, 전역적으로 적극 사용한다. 랜딩 히어로뿐 아니라 버튼 · 진행바 · 카드 강조 테두리/글로우 등에도 브랜드 포인트 컬러 그라데이션을 쓴다. 과거의 "정보 화면은 단색만" 같은 절제 원칙은 더 이상 유효하지 않다.

원칙:

- **브랜드 포인트 컬러 3색(`#414AFF` 블루 → `#8920FF` 퍼플 → `#34AD5C` 그린)을 조합한 그라데이션을 버튼, 진행 표시, 강조 테두리/글로우, 배지 등 주요 인터랙션 요소에 적극적으로 쓴다.** 아무데나 무작위로 까는 게 아니라 CTA·상태 표시처럼 시선을 끌어야 하는 지점에 의도적으로 쓴다.
- **다크 배경 위에서 그라데이션 대비가 선명하게 보이도록 한다** — 그림자/글로우(box-shadow)로 입체감을 더해도 좋다.
- **타이포그래피와 그리드 기본기는 유지한다.** 그라데이션을 쓴다고 여백·정렬·위계까지 흐트러뜨리지 않는다.
- **모션은 정보 전달 목적일 때 우선하되, 장식성 모션(은은한 글로우 펄스 등)도 이제 허용.** 다만 과도해서 키오스크 완주율을 해치지 않게 한다.
- **테마: 다크 우선.** 다크 테마를 먼저 완성하고, 라이트 테마는 이후 추가해서 토글 가능하게 한다(다크가 fallback/기본값).
- **포인트 컬러**: `#000000`(블랙, 배경/텍스트 기준), `#414AFF`(블루), `#8920FF`(퍼플), `#34AD5C`(그린) — 이 3색 조합 그라데이션이 기본 무드가 됐다. 필요하면 Google Cloud 브랜드 컬러를 보조적으로 섞어 쓴다.

## 확정된 결정 (2026-09-17, `docs/consent-docs/2026-09-17-decisions.md` 참조)

- **네비게이션**: 상단 메뉴.
- **화면 전환**: `react-router-dom` URL 라우트로 분리. **최종 라우트(2026-09-17 정리 완료)**: `/`(홈, 순수 히어로만) · `/architecture` · `/experience`(6개 도메인 그리드 + 체험 순서 3단계) · `/playground/:domainId`(실제 데모). "도메인 선택"이라는 별도 탭은 없다 — "체험" 탭이 `/experience`로 직접 연결되고, 거기서 카드를 고르면 `/playground/:domainId`로 이어진다. 상단 메뉴는 홈/AI 아키텍처/체험 3개.
- **"AI는 어떻게 판단하는가"(아키텍처) 화면**: 구현 우선순위 최하위. 나중에 A(단순화된 정적 다이어그램)와 B(아이콘/텍스트 카드) 두 시안을 실제로 만들어서 사용자가 눈으로 보고 고른다 — 지금 코드로 임의 확정하지 말 것.
- **데모 시나리오 단계 수**: 도메인마다 유동적으로 3~4단계(획일적으로 4단계 통일 아님). 가장 임팩트 있는 단계만 남긴다.
- **1차 버전 스코프**: 6개 도메인 전부 포함. "R&D 신소재 품질 검증 세트"·"글로벌 원자재 MD 공급망 세트"는 제외.
- **도메인 카드 구성**: 숫자/규제 태그 없이, 아이콘 + 에이전트 이름 + 짧은 사용 예시만.
- **외부 Gemini Enterprise 채팅 연동 (최종 확정)**: iframe은 `X-Frame-Options: SAMEORIGIN`으로 원천 차단됨을 확인(스파이크 완료). 위치 지정 팝업은 "윈도우창 느낌"이 나서 키오스크 톤에 안 맞는다고 판단해 기각. **Chrome 145+ 네이티브 stacked split view**로 확정: 키오스크 셋업 시 운영자가 위(우리 앱)/아래(채팅용, `window.name='ge-chat-pane'`)로 수동 1회 분할 구성 → "채팅 시작" 버튼은 `window.open(agentSessionUrl, 'ge-chat-pane')`만 호출하면 이미 보이는 아래쪽 영역 내용이 그 자리에서 바뀜(새 창 없음). Split view 자체는 Chrome 공개 API로 생성할 수 없어 자동화 불가 — 운영자가 하루 시작 시 수동 셋업. 상세: `plan.md` Phase 1.
- **무인 대기 자동 초기화**: 1분간 입력이 없으면 "계속하시겠습니까?" 같은 확인 팝업(예/아니오)을 띄우고, 응답이 없거나 "아니오"를 선택하면 처음 화면으로 리셋한다.
- **데모 첨부 파일**: ~~기존 파일을 그대로 쓰지 않고, 축소된 3~4단계 시나리오에 맞게 일부 새로 제작/편집한다~~ → **Phase 5에서 뒤집힘**: 원본 사이트의 실제 첨부파일이 그대로 존재함을 확인해서, 신규 제작 없이 원본 23개 파일을 그대로 `public/demo-data/<domainId>/`에 다운로드해서 재사용했다(회사명/지명 리브랜딩도 안 함, Vision 이미지 개수도 원본 그대로 2개 유지 — `docs/consent-docs/2026-09-17-phase5-decisions.md`).
- **테마/컬러**: 위 "디자인 방향" 항목 참조.

## 더 확인이 필요한 것

- 아키텍처 화면의 최종 시안(A vs B)은 두 개를 실제로 만들어 비교한 뒤 결정한다(위 참조).
- `window.open(url, 'ge-chat-pane')`으로 split view 탭을 내비게이션했을 때 분할이 유지되는지 실측 미검증 — Phase 9 QA 또는 키오스크 셋업 리허설에서 반드시 확인.
- 그 외 새로 발견되는 결정 대기 항목은 `docs/consent-docs/`에 새 문서로 추가한다.

## 기술 스택

- Vite + React 19 + TypeScript, `react-router-dom` v7 (라우팅), Tailwind CSS v4
- 테스트: Vitest + Testing Library (`npm test`), 린트: ESLint (`npm run lint`), 포맷: Prettier (`npm run format`)
- 홈/도메인 체험/AI 아키텍처/데모 플레이그라운드까지 Phase 0~5.2가 구현 완료된 상태(2026-09-17 기준). `docs`/`plan.md` 참조. `/architecture`만 아직 placeholder(Phase 8에서 시안 A/B 확정 예정). 참고 사이트의 소스코드는 이 리포에 없다(별도 배포물이며, 이번 작업은 "새로 설계해서 만드는 것"이다).
- `pretendard` npm 패키지로 폰트를 자체 호스팅한다(CDN 아님). **이 리포는 `pnpm` 기반이라 새 패키지 추가 시 `npm install`이 아니라 `pnpm add`를 써야 한다** — 섞어 쓰면 arborist 오류로 `node_modules`가 깨진다(실제로 한 번 겪음).

## 작업 컨벤션

- 화면 단위는 `react-router-dom` 라우트로 분리한다: `/`(홈, 기본 경로), `/architecture`(AI 판단 방식 설명, 최후순위 구현), `/experience`(도메인 그리드 + 체험 순서), `/playground/:domainId`(실제 데모, 하단 채팅 split view 연동).
- "체험" 탭의 활성 표시는 `SelectedDomainContext` 같은 전역 상태로 만들지 않는다 — `/experience`와 `/playground/:domainId`가 이제 항상 존재하는 실제 라우트라 조건부 상태가 필요 없다(예전엔 필요해서 만들었다가 나중에 지웠음, `AppLayout`에서 `useLocation` 경로 접두사 확인으로 충분).
- 세로(포트레이트) 40인치 화면 기준으로 레이아웃을 짠다. 가로 탭 바, 가로로 긴 3D 다이어그램 등 기존 사이트의 와이드스크린 전제 UI는 그대로 가져오지 않는다. **단, 도메인 카드는 예외(2026-09-17 뒤집힘)** — 그리드로 반응형 열 배치를 시도했다가 사용자가 "구리다"며 원본 사이트(`#domains`)의 가로 스크롤 캐러셀 + 고정폭 카드 + 마우스 틸트/스포트라이트 효과를 그대로 가져오라고 함. `src/components/DomainCard.tsx`(`.tilt-card`, `src/index.css`) 참조.
- 주석은 WHY가 명확하지 않은 코드에만 최소한으로 남긴다.
- 불필요한 추상화나 이번 스코프 밖의 기능을 미리 만들지 않는다.
