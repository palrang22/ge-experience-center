function ArchitecturePage() {
  return (
    <section className="mx-auto flex max-w-6xl flex-col gap-4 px-6 py-10">
      <p className="font-mono text-xs tracking-[0.2em] text-brand-purple">AI ARCHITECTURE</p>
      <h1 className="text-3xl font-semibold tracking-tight">AI는 어떻게 판단하는가</h1>
      <p className="max-w-xl text-text-secondary">
        이 화면은 구현 우선순위상 가장 나중에 만들어집니다(Phase 8). 정적 다이어그램(시안 A)과
        아이콘 카드(시안 B) 두 가지를 만들어 비교한 뒤 하나로 확정합니다.
      </p>
    </section>
  )
}

export default ArchitecturePage
